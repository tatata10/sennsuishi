# 潜水士学習アプリ・環境構築＆動作確認マニュアル

## 1. 事前準備
このアプリを実行するには、以下の環境が必要です。

*   **Flutter SDK**: インストール済みであること (`flutter --version` で確認)
*   **VS Code**: 推奨エディタ（Flutter拡張機能入り）
*   **Windows 開発者モード**: ONであること（設定 > プライバシーとセキュリティ > 開発者向け > 開発者モード）

## 2. 環境構築手順（セットアップ）

現在のフォルダにはソースコード (`lib/` など) のみが存在します。アプリとして実行可能にするために、以下のコマンドを順番に実行してください。

### 手順①：プラットフォームフォルダの生成
ターミナルで以下のコマンドを実行し、Android/iOS/Web用のプロジェクトファイルを生成します。

```bash
flutter create . --project-name=sensuishi_app
```
※ 「Recreating project...」と表示されれば成功です。`android` フォルダや `ios` フォルダが作成されます。

### 手順②：依存ライブラリのインストール
必要なパッケージ（Riverpod, SQLiteなど）をダウンロードします。

```bash
flutter pub get
```

## 3. アプリの起動と動作確認

### 起動方法
エミュレーターを起動した状態で、以下のコマンドを実行するか、VS Codeの「Run and Debug」から起動してください。

```bash
flutter run
```

### 動作確認シナリオ（SQLite連携の確認）

データベースが正しく機能しているか、以下の手順で確認してください。

1.  **ホーム画面の確認**
    *   アプリが起動し、進捗グラフと4つのメニューボタンが表示されることを確認します。

2.  **「お気に入り・弱点」の確認（初期状態）**
    *   右下の「お気に入り・弱点」ボタンをタップします。
    *   **「弱点問題はありません！」** と表示されるはずです。
    *   左上の「戻る」ボタンでホームに戻ります。

3.  **クイズで「わざと間違える」**
    *   左上の「年度別 過去問」ボタンをタップします。
    *   クイズが表示されます（例：「減圧症の予防...」）。
    *   **わざと間違った選択肢**を選んでください（解説で「不正解」と出ます）。
    *   「次の問題へ」を押すか、左上の「×」ボタンでホームに戻ります。

4.  **保存データの確認**
    *   もう一度、ホーム画面右下の「お気に入り・弱点」ボタンをタップします。
    *   先ほど間違えた問題がリストに表示されていれば、**SQLiteへの保存機能は正常に動作しています**。

## トラブルシューティング

*   **エラーが出る場合**:
    *   `flutter clean` を実行してから、もう一度 `flutter pub get` → `flutter run` を試してください。
*   **データベースエラー**:
    *   アプリをアンインストールしてから再インストール（再ビルド）すると、データベースがリセットされ解決することがあります。
